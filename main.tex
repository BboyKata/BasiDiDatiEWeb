\documentclass{article}
\usepackage{graphicx} % Required for inserting images
\usepackage{amsmath}
\usepackage{cancel}
\usepackage{enumitem}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{wrapfig}
\usepackage{comment}
\usepackage{array}
\usepackage{float}
\usepackage{subcaption}
\usepackage{amsmath}
\usepackage{forest}
%\usepackage{table}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{shapes, intersections}
\usepackage{multicol}
\usepackage{listings} %for code
\lstdefinestyle{sqlstyle}{
	language=SQL,
	backgroundcolor=\color{lightgray!10},
	basicstyle=\ttfamily\small,
	keywordstyle=\color{blue}\bfseries,
	commentstyle=\color{gray},
	stringstyle=\color{orange!90!black},
	numbers=left,
	numberstyle=\tiny,
	stepnumber=1,
	frame=lines,
	showstringspaces=false,
	tabsize=4,
	breaklines=true
}

\setlength{\parindent}{0pt}
\geometry{
	a4paper,
	total={170mm,257mm},
	left=20mm,
	top=20mm,
}
\title{\Huge \textbf{Basi di dati}
	\small
	
	
	\textit{(aiuto!!!)}}
\author{Matteo Balzerani}
\date{October 2025}

\newcommand{\sitemize}[1]{%
	\begin{itemize}[label=$\diamond$]
		#1
	\end{itemize}
}

\newcommand{\mybox}[1]{%
	\vspace{1em}%
	\noindent\fbox{\colorbox{gray!20}{%
			\begin{minipage}{\dimexpr\textwidth-2\fboxsep}
				#1
			\end{minipage}%
	}}%
	\vspace{1em}%
}

\newcounter{esempio}
\newenvironment{esempio}{
	\refstepcounter{esempio}%
	\noindent\textbf{Esempio \theesempio:}
}{\par}

\graphicspath{{graphics/}}

\begin{document}
	
	\maketitle
	\small
	\begin{multicols}{2}
		\tableofcontents  % <-- Questo genera l'indice
	\end{multicols}
	\newpage
	\normalsize
	\paragraph{Descrizione Esame}
	\begin{itemize}
		\item \textbf{Modulo di Teoria}
		Prova scritta che consiste in domande ed esercizi sugli argomenti trattati. Possibilità di ripetere la prova. In caso di ripetizione della prova, l'ultimo voto ottenuto sostituisce il precedente. 
		
		\item \textbf{Modulo di Laboratorio}
		Prova scritta che consiste in esercizi sugli argomenti trattati. Il voto finale dell'insegnamento di Basi di Dati e Web sarà dato dalla media tra il voto
		della prova scritta di teoria e il voto delle prova scritta di laboratorio. Il voto complessivo verrà pubblicato anche sulla piattaforma MyUnivr e dopo l’accettazione verrà verbalizzato.
	\end{itemize}
	\part{Teoria}
	\section{Introduzione}
	\paragraph{Cos'è un DBMS?} Sistema (prodotto software) in grado di gestire collezioni di dati che siano:
	\begin{itemize}
		\item \textbf{grandi} (di dimensioni (molto) maggiori della memoria centrale dei sistemi di
		calcolo utilizzati). La grandezza richiede che tale gestione sia sofisticata (non
		possiamo caricare tutto in memoria principale e poi
		riscaricare).
		\item \textbf{persistenti} (con un periodo di vita indipendente dalle singole esecuzioni dei
		programmi che le utilizzano). La persistenza richiede una gestione in memoria
		secondaria.
		\item \textbf{condivise} (utilizzate da applicazioni diverse)
			\end{itemize}
		garantendo \textbf{affidabilità} (resistenza a malfunzionamenti
		hardware e software) e \textbf{privatezza} (con una disciplina e un
		controllo degli accessi).
		Come ogni prodotto informatico, un DBMS deve essere
		\textbf{efficiente} (utilizzando al meglio le risorse di spazio e tempo
		del sistema) ed \textbf{efficace} (rendendo produttive le attività dei
		suoi utilizzatori).
		\paragraph{Architettura a livelli dei DBMS}
		I DBMS sono organizzati secondo una architettura a livelli\begin{itemize}
			\item Livello \textbf{LOGICO}: a cui fanno riferimento gli utenti (SQL).
			\item Livello \textbf{FISICO}: stabilisce l’effettiva implementazione in modo efficiente
			
			\textit{Esempio: le relazioni del modello relazionale dei dati vengono realizzate utilizzando varie possibili strutture fisiche}
					\end{itemize} 
			La proprietà di INDIPENDENZA dei dati permette agli utenti di ignorare le strutture fisiche
			– SQL fa riferimento al livello logico
			– Il DBMS si preoccupa di tradurre le operazioni al livello più basso, utilizzando
			in modo efficiente le strutture fisiche. Gli utenti vedono il modello logico (relazionale).
			
			I dati sono in \textbf{memoria secondaria}, le strutture logiche non sarebbe efficienti in memoria secondaria: servono strutture fisiche opportune.
			La memoria secondaria è molto più lenta della memoria principale: serve un'interazione fra memoria principale e secondaria che limiti il più	possibile gli accessi alla secondaria.
			
			\begin{figure}[h!]
				\centering
				\includegraphics[width=0.4\linewidth]{screenshot001}
				\caption{Architettura della gestione degli accessi e delle interrogazioni in un DBMS}
			\end{figure}
			
			\subsection{Moduli di un DBMS}
			\mybox{\textbf{Domanda d'esame: \textit{Si descrivano i moduli che compongono il DBSM descrivendo brevemente le funzioni di ciascuno:}} risposta nelllo schema sottostante. Bisogna parlare di ciascun modulo brevemente, facendo l'esempio di una query dall'alto verso il basso e viceversa.}
			
			\begin{figure}[h!]
				\centering
				\includegraphics[width=0.7\linewidth]{graphics/screenshot002}
				\caption{}
				\label{fig:screenshot002}
			\end{figure}
			
			
			
			\paragraph{Gestore delle interrogazioni} Prende la query logica e la traduce in una forma interna. Le trasforma per renderle più efficienti e le realizza in termini di \textbf{operazioni di basso livello} che fanno riferimento alla struttura fisica dei file.
			
			\paragraph{Gestore dei metodi di accesso} Esegue le operazioni di basso livello. Il modulo conosce i dettagli della struttura fisica e quindi trasforma le richieste in operazioni di lettura e scrittura di dati in memoria secondaria. Tali operazioni vengono mediate dal GESTORE DEL BUFFER.
			
			\paragraph{Gestore del buffer} Ha la responsabilità di manetenere temporaneamente porzioni della BD in memoria centrale e invia le richieste di lettura e scrittura fisica al \textbf{gestore della memoria secondaria.}
			
			\paragraph{Transazioni} le basi di dati si basano sul concetto di transazione (insieme di operazioni), che deve essere \textbf{atomiche} (o tutto o niente) e \textbf{definitive} (dopo che è andata a buon fine, deve rimanere permanente la transazione eseguita).
			\subsection{Condivisione di una BD}
			Una BD è una risorsa \textit{integrata}, \textit{condivisa} fra le varie applicazioni, e di conseguenza vi sono attività diverse su dati in parte condivisi; questo richiede \textbf{meccanismi di autorizzazione} e attività multi-utente su dati condivisi:
			richiedono \textbf{controllo della concorrenza.} L'accesso concorrente in scrittura deve essere regolato.
			
			\begin{figure}[h!]
				\centering
				\includegraphics[width=0.4\linewidth]{graphics/screenshot003}
			\end{figure}
			
			Il \textbf{gestore della concorrenza e il gestore della affidabilità sono sottomoduli del gestore delle transazioni} (DBMS sistema informativo transazionale.) Significa gestire la concorrenza e gestire l'affidabilità.
			
			\mybox{\textbf{Domanda:} \textit{Quali sono i moduli che mi permettono di gestire le transazioni in un DBMS?}}
			
			\subsection{Gestione della memoria secondaria}
			
			Il costo di un accesso a memoria secondaria è quattro o più ordini di grandezza maggiore di quello per operazioni in memoria centrale. Nelle applicazioni "I/O bound" (cioè con molti accessi a memoria secondaria e relativamente poche operazioni) il
			costo dipende esclusivamente dal numero di accessi a memoria secondaria. Nei DBMS l’interazione tra memoria centrale e secondaria è realizzata attraverso un buffer.
			
			\subsubsection{Buffer}
			\begin{itemize}
				\item Area di memoria centrale, gestita dal DBMS (preallocata) e condivisa fra le
				transazioni.
				\item Organizzato in \textbf{pagine} di dimensioni pari o multiple di quelle dei blocchi di
				memoria secondaria.
				 (Per semplicità ipotizzeremo che ciascuna pagina corrisponda ad un blocco)
				\item L’allocazione di buffer sempre più grandi, riduce il numero di accessi alla
				memoria secondaria
				\item La gestione ottimale del buffer è un aspetto fondamentale nelle BD (rispetto
				alle prestazioni). Il buffer è importantissimo per via della grande differenza di tempo di accesso fra
				memoria centrale e memoria secondaria.
				
			\end{itemize}
			
			\subsubsection{Gestione del buffer}
			\paragraph{Scopo della gestione del buffer}\begin{itemize}
				\item \textbf{Ridurre il numero di accessi alla memoria secondaria} \textit{GARANTENDO L'AFFIDABILITÀ.} \sitemize{\item 
				In caso di \textbf{lettura}, se la pagina è già presente nel buffer, non è necessario
				accedere alla memoria secondaria.
				\item In caso di \textbf{scrittura}, il gestore del buffer può decidere di differire la scrittura
				fisica (ammesso che ciò sia compatibile con la gestione dell’affidabilità)}
				
				\item Le politiche di gestione del buffer assomigliano a quelle
				della gestione della memoria centrale da parte dei Sistemi
				Operativi: obbediscono allo stesso principio, detto di località, in base al quale i dati
				referenziati di recente hanno una maggiore probabilità di essere referenziati
				nuovamente in futuro
			\end{itemize}
			
			Il buffer offre ai moduli di livello più alto una interfaccia complessa, non si limita alle richieste di lettura e scrittura. Ha bisogno delle informazioni sul \textbf{prevedibile riutilizzo delle pagine} (per evitarne la riapertura); Ha bisogno delle informazioni sulla necessità di \textbf{effettuare immediatamente gli aggiornamenti} (per evitare la perdita in caso di guasto). Per la gestione dei buffer sono necessarie le seguenti
			informazioni: \begin{itemize}
				\item Un direttorio che per ogni pagina mantiene (ad esempio) il file fisico e il numero del blocco ad essa corrispondente.
				\item Per ogni pagina del buffer il gestore mantiene due variabili di stato: \textbf{un contatore }che indica quanti programmi utilizzano la pagina e un \textbf{bit} che indica se la pagina è “sporca”, cioè se è stata modificata (\textit{dirty bit}).
			\end{itemize}
		
		\paragraph{Funzioni del buffer manager}
		Le politiche sono simili a quelle relative alla gestione della memoria da parte dei sistemi operativi. 
		
		\mybox{\textbf{Principio di "località dei dati”:} è alta la probabilità di dover riutilizzare i dati attualmente in uso.
		
		\textbf{Legge 80-20:} l'80\% delle operazioni utilizza sempre lo stesso 20\% dei dati.
		}
		
		\paragraph{Operazioni per la gestione del buffer:} \begin{itemize}
			\item \textbf{Fix:} Operazione che permette a un processo di accedere a una pagina, caricandola dal disco solo se non è già presente nel buffer. Incrementa il contatore dei riferimenti (pin count) della pagina per bloccarla ed evitare che venga rimossa mentre è in uso. Restituisce il riferimento (indirizzo) alla pagina nel buffer.
			
			\textbf{Passi dell'operazione:}
			\begin{enumerate}
				\item \textbf{Cerca la pagina nel buffer.}
				\begin{itemize}
					\item Se è presente (grazie al principio di località, ciò avviene abbastanza spesso): incrementa il contatore e restituisce l'indirizzo.
					\item Se non è presente: procedi al passo successivo.
				\end{itemize}
				
				\item \textbf{Cerca un frame libero} (cioè una pagina con contatore = 0, non usata da nessun processo).
				\begin{itemize}
					\item Se lo trova: 
					\begin{itemize}
						\item Se il dirty bit (bit di stato modificato) è 1, riscrive la pagina in memoria di massa prima di sovrascriverla.
						\item Carica la nuova pagina dal disco, imposta il contatore a 1, restituisce l'indirizzo.
					\end{itemize}
					\item Se non trova frame liberi (tutti i contatori sono $> 0$): adotta una delle due strategie:
					\begin{itemize}
						\item \textbf{Steal:} seleziona una pagina "vittima" (es. con algoritmo LRU), la scrive su disco se modificata, la sovrascrive con la nuova pagina, restituisce l'indirizzo.
						\item \textbf{No-steal:} mette l'operazione in attesa finché non si libera un frame (cioè qualche processo esegue Unfix portando un contatore a 0).
					\end{itemize}
				\end{itemize}
			\end{enumerate}
		
			
			\item \textbf{setDirty}
			Comunica al buffer manager che la pagina è stata modificata. Viene modificato il BIT di stato relativo.
			\item \textbf{Unfix:} indica che la transazione ha concluso l'utilizzo della pagina, decrementa il contatore associato alla pagina.
			\item \textbf{Force:} Trasferisce in modo sincrono una pagina in memoria secondaria \textbf{su richiesta del gestore dell'affidabilità} (non del gestore degli accessi) per garantire che alcuni dati non vengano persi. Memorizza in maniera permanente le modifiche per garantire l'affidabilità.
		\end{itemize}
	
		\mybox{La scrittura in memoria secondaria può avvenire in due
			modi: \begin{itemize}
				\item in modo \textbf{sincrono} quando è richiesto esplicitamente con una force
				\item in modo \textbf{asincrono} (indipendente dall’applicazione) quando il buffer
				manager lo ritiene opportuno (o necessario). In particolare, può decidere di anticipare o posticipare scritture per coordinarle e/o sfruttare la disponibilità dei dispositivi
			\end{itemize}
		}
		
		\newpage
		\section{Gestione delle TUPLE nelle pagine}
		Ciascuna pagina contiene l'\textit{informazione utile}, cioè i dati veri e proprio, e l'\textit{informazione di controllo}, per accedere all'informazione utile. I blocchi (componenti fisici di un file) e i record (componenti logici) hanno dimensioni in generale diverse: la dimensione del blocco \textit{dipende dal file system} e la dimensione del record dipende dalle esigenze del record.
		\paragraph{Fattore di blocco} Il numero di \textbf{record} in un \textbf{blocco} è pari al rapporto fra la dimensione del blocco $L_B$ e la dimensione media dei record $L_R$. Se $L_B > L_R$ possiamo avere più record in un blocco:$ \lfloor L_B / L_R \rfloor$. Lo spazio residuo può essere \textit{utilizzato}, cioè \textbf{spanned} (impaccato) o non utilizzato, cioè \textbf{unspanned}
		
		\begin{figure}[h!]
			\centering
			\includegraphics[width=0.9\linewidth]{graphics/screenshot004}
			\label{fig:screenshot004}
		\end{figure}
		
		Abbiamo diverse strutture primarie per l'organizzazione di file, e stabiliscono il \textit{criterio secondo cui le tuple sono disposte nel file}. Ci sono tre categorie principali di strutture:\begin{itemize}
			\item \textbf{Sequenziali:} disposizione consecuitva delle tuple
			\item \textbf{Ad accesso calcolato - HASH:} disposizione in posizioni che dipendono dall'esecuzione di un algoritmo
			\item \textbf{Ad albero:} usate soprattutto come \textbf{strutture secondarie.}
		\end{itemize}
		
		\subsection{Strutture sequenziali}
		Nelle strutture sequenziali un file è costituito da vari blocchi di memoria "logicamente" consecutivi. Le tuple vengono inserite nei blocchi rispettando una sequenza. 
		\subsubsection{Struttura seriale (disordinata)} È la struttura sequenziale più semplice ma anche più diffusa, le tuple venogno inserite nel file nell'ordine in cui si presentano; non esiste un ordinamento basato su qualche valore. Viene anche chiamata \textbf{"entry sequenced"}/file heap/file disordinato. Gli inserimento vengono effettuati \textit{in coda} o \textit{al posto di record cancellati}. Le \textbf{cancellazioni} si effettuano marcando il record come "cancellato". le tuple vengono periodicamente riorganizzate per eliminare lo spazio sprecato. 
		
		È molto diffusa nelle basi di dati relazionali, associata ad inidici secondari. È molto efficiente per le operazioni di inserimento, poiché è sufficiente mantenere un riferimento all'ultimo blocco. Le \textit{ricerche sono poco efficienti:} richiedono una scansione sequenziale di tutti i record, con un costo lineare nel numero di blocchi del file. Per questo motivo sono spesso usate insieme a strutture secondarie usate per favorire gli accessi.
		\subsubsection{Struttura ad array} È possibile solo quando le tuple hanno lunghezza fissa: al file viene associato un numero di blocchi contigui; ogni \textit{blocco} ha un numero fisso di posizioni disponibili per le tuple; ogni \textit{tupla} ha un indice \textit{i} e viene posta nella posizione dell'indice associato. Questa struttura non è quasi mai utilizzata dai DBMS anche se sono efficienti per la ricerca.
		
		\subsubsection{Strutture ordinate (clustered)}
		Prevedono la memorizzazione dei record secondo un \textbf{ordinamento fisico coerente con l'ordinamento di un campo detto CHIAVE.} Permettono ricerche binarie, ma solo fino ad un certo punto, poiché senza indici non conosci la disposizione dei blocchi, quindi non puoi \textit{calcolare la posizione a metà di un file}. Per questo, nelle basi di dati relazionali si utilizzano quasi solo in combinazioni con indici come \textbf{file ISAM} o file ordinati con \textit{indice primario.} ISAM è l'acronimo di \textit{Indexed Sequential Access Method}.

		\subsection{Strutture ad accesso calcolato: HASH}
		Le tuple sono collocate in posizioni che dipendono dall'esecuziuone di un algoritmo. Permettono un accesso diretto molto efficiente. La tecnica si basa su quella utilizzata per le tavole hash in memoria centrale.
		
		\textbf{Obiettivo}: accesso diretto ad un insieme di record sulla base del valore di un campo (detto chiave, che per semplicità supponiamo identificante, ma non è necessario). \begin{itemize}
			\item Se i possibili valori della chiave sono in numero paragonabile al numero di record (e corrispondono ad un "tipo indice") allora usiamo un array. \textit{Esempio: università con 1000 studenti e numeri di matricola compresi fra 1 e 1000 o poco più e file con tutti gli studenti}. 
			\item Se i possibili valori della chiave sono molti di più di quelli effettivamente utilizzati, non possiamo usare l'array (spreco); \textit{ad esempio: 40 studenti e numero di matricola di 6 cifre (un milione di possibili chiavi)}. 
		\end{itemize}
			
		Volendo continuare ad usare qualcosa di simile ad un array,ma senza sprecare spazio, possiamo pensare di
		trasformare i valori della chiave in possibili indici di un array: \textbf{Funzione HASH}.
		\paragraph{Funzione HASH} Associa ad ogni valore della chiave un "indirizzo", in uno spazio di
		dimensione paragonabile (leggermente superiore) rispetto a quello strettamente necessario. La funzione non può essere iniettiva, quindi il numero di possibili chiavi è molto maggiore del numero di possibili indirizzi ("lo
		spazio delle chiavi è più grande dello spazio degli indirizzi"), ed esiste la possibilità di collisioni (chiavi diverse che corrispondono allo stesso indirizzo). Le buone funzioni hash distribuiscono in modo causale e uniforme,
		riducendo le probabilità di collisione (che si riduce aumentando lo spazio ridondante).  Varie tecniche: posizioni successive disponibili, tabella di overflow (gestita in forma collegata), funzioni hash "alternative".
		
		\begin{figure}[h!]
			\centering
			\includegraphics[width=0.7\linewidth]{graphics/screenshot005}
		\end{figure}
		
		
		\mybox{
\textbf{Nota:}
\begin{itemize}
			\item le collisioni ci sono (quasi) sempre
	\item le collisioni multiple hanno probabilità che decresce al crescere della
	molteplicità
	\item la molteplicità media delle collisioni è molto bassa 
\end{itemize}

		}
		
		\subsubsection{File HASH}
		 L'idea è la stessa della tavola hash, ma si basa sull'organizzazione in blocchi. L’operazione di costo unitario è l’accesso ad un blocco che può contenere diversi record. I blocchi sono riempiti solo in parte: \begin{itemize}
		 	\item \textbf{Fattore di riempimento FdR:} frazione dello spazio fisico disponibile mediamente utilizzato. \textit{Ad esempio: 40 record di studenti su 50 posizioni disponibili} $\to FdR = 40/50 = 0.80$
		 	\item \textbf{Fattore di blocco FdB:} rapporto fra la dimensione del blocco $L_B$ e la dimensione del record $L_R$: $ FdB = \lfloor L_B / L_R \rfloor$
		 \end{itemize}
		 Se supponiamo di avere:\begin{itemize}
		 	\item \textbf{T:} numero di tuple previsto per il file
		 	\item \textbf{FdB:} fattore di blocco
		 	\item \textbf{FdR:} fattore di riempimento
		 \end{itemize}
		 Allora il file può avere un numero di \textbf{Blocchi B} pari a $B = \lceil \dfrac{T}{FdR\cdot FdB} \rceil$
		 
		 La funzione di HASH ritorna un numero compreso tra 0 e $B-1$, interpretato come il numero del blocco nell'ambito dei blocchi allocati al file.
		 
		 \begin{figure}[h!]
		 	\centering
		 	\includegraphics[width=0.2\linewidth]{graphics/screenshot006}
		 \end{figure}
		 
		 I \textbf{blocchi allocati al file} sono \textbf{B}, la funzione di HASH ritorna un numero tra \textbf{0 e B-1}. In caso di collisione i record vengono semplicemente allocati nel blocco fino ad esaurimento dello
		 spazio libero; vengono accettate FdB (numero di record nel blocco) collisioni. Quando un blocco viene esaurito viene allocato un nuovo blocco collegato al precedente ed il record viene disposto nel nuovo blocco: \textit{CATENE DI OVERFLOW}.
		 
\mybox{		 È \textbf{l'organizzazione più efficiente per l'accesso diretto basato su valori della chiave} con condizioni di uguaglianza (accesso puntuale): il costo medio è di poco superiore all'unità e il caso peggiore è molto costoso ma talmente improbabile da poter essere ignorato. \textit{Le collisioni (overflow) sono di solito gestite con blocchi
		collegati}. \textbf{Non è efficiente per ricerche basate su intervalli}: le funzioni HASH tendono a collocare valori vicini su blocchi diversi, ad esempio: ricerca degli studenti con matricola compresa tra 17010 e 17029 richiede 20 accessi diretti uno per ciascun valore dell’intervallo.}
		 
		 \begin{figure}[h!]
		 	\centering
		 	\includegraphics[width=0.7\linewidth]{graphics/screenshot007}
		 	\caption{Esempio File hash. Numero tuple T = 40. Numero Blocchi B = 5.FdB = 10 (10 posizioni in ogni blocco). Posizioni disponibili = 50. FdR = 0.80. La funzione di HASH restituisce un numero tra 0 e 4. Numero di Overflow 2 → num. medio di accessi: 1,05}
		 \end{figure}
		 
		 \newpage
		 \subsection{Strutture ad Albero}
		 L'organizzazione ad albero può essere utilizzata per realizzare \textit{strutture primarie (contenenti i dati)} e \textit{strutture secondarie}, che favoriscono l'accesso ai dati senza contenere i dati stessi.
		 
		 \mybox{È leggermente \textit{meno efficiente} per accessi puntuali, ma mantiene la stessa efficienza anche per accessi basati su interalli.}
		 \subsubsection{Indici di file}
		 \textbf{Indice:} struttura ausiliaria per l'\textit{accesso efficiente} ai record di un file sulla base dei valori di un campo (o di una concatenazione di campi) detto \textbf{chiave} (o, meglio, pseudochiave, perché non è necessariamente identificante).
		 
		 \mybox{\textbf{Idea fondamentale:} l'indice analitico di un libro, lista di coppie (termine, pagina), ordinata alfabeticamente sui termini, posta in fondo al libro e separabile da esso.}
		 
		 Un indice \textit{I} di un file \textit{f} è un \textbf{altro file}, con \textbf{record a due campi:} \textit{chiave} e \textit{indirizzo} (dei record di \textit{f} o dei relativi blocchi), \textit{ordinato} secondo i valori della chiave.
		 
		\paragraph{Tipi di indice}
		A seconda della popolazione dell'indice abbiamo:
		\begin{itemize}
			\item \textbf{Indice denso:} contiene un record per ciascun valore del campo chiave.
			\item \textbf{Indice sparso:} contiene un numero di record \textit{inferiore} rispetto al numero di valori diversi dal campo chiave, cioè \textit{esistono valori della chiave che NON sono presenti nell'indice.}
		\end{itemize}
		Vi è poi la differenza tra:\begin{itemize}
			\item \textbf{Indice primario:} l'indice contiene al suo interno i dati o è realizzato su un file ordinato sullo stesso capmo su cui è definito l'indice stesso. \textit{Può essere sparso.}
			
			\textit{Esempio: indice generale di un libro, determina la posizione dei capitoli che sono i dati del libro.}
			
			Un indice primario, grazie all'ordinamento, può essere realizzato "puntando" ad un solo record di un blocco (indice sparso), poiché gli altri record sono posti in modo adiacente a quello puntato.
			
			\item \textbf{Indice secondario:} l'indice è definito su un campo con ordinamento diverso da quello di memorizzazione. \textbf{DEVE essere denso.}
			
			\textit{Esempio: indice analitico di un libro, che è l'indice secondario che garantisce l'accesso senza contenere i dati.}
			
			Un indice secondario deve contenere tutti i rifermenti a tutti i valori della chiave (indice denso), infatti valori consecutivi della chiave possono trovarsi in blocchi diversi.
			
		\end{itemize}
		
		I benefici legati alla presenza di indici secondari sono molto più sensibili. \textit{Ogni file può avere al più un indice primario e un numero qualunque di indici secondari (su campi diversi).}.
		
		\textbf{Un file hash non può avere un indice primario: Il criterio di memorizzazione è stabilito dalla funzione di HASH e quindi non può essere determinato dall’indice.}
			
	
		\noindent
		\begin{minipage}{0.48\linewidth}
			\centering
			\includegraphics[width=\linewidth]{graphics/screenshot008}
		\end{minipage}
		\hfill
		\begin{minipage}{0.48\linewidth}
			\centering
			\includegraphics[width=\linewidth]{graphics/screenshot009}
		\end{minipage}
		
		\textit{Domanda d'esame: si dia la definizione di indice, che cos'è un indice primario, secondario, denso e sparso.}
		
		\textit{Risposta:}l'indice è un file che ha un insieme di coppie con valore di chiave e indirizzo, se l'indice contiene le informazioni è un indice primario, se non contiene le informazioni è secondario.
		Se contiene un Record per ogni valore della chiave è denso, se no è sparso. Gli indici primari possono essere sparsi. Gli indici secondari devono essere densi.
		
		\paragraph{Prestazioni degli indici}
		\begin{itemize}
			\item Accesso diretto sulla chiave efficiente, sia puntuale sia per intervalli
			\item scansione sequenziale ordinata efficiente: tutti gli indici ( in particolare quelli secondari) forniscono un \textbf{ordinamento logico} sui record dei file.
			\item Modifiche della chiave, inserimenti, eliminazioni \textit{inefficienti}: esistono tecniche per alleviare i problemi: \textit{file o blocchi di overflow, marcatura per le eliminiazioni, riempimento parziale, blocchi collegati (non contigui), riorganizzazioni periodiche...}
			
		\end{itemize}
		
		\mybox{\textbf{Osservazione sugli indici secondari:} si possono usare, come detto, puntatori ai blocchi oppure puntatori ai record:
			\begin{itemize}
				\item I puntatori ai blocchi sono più compatti
				\item I puntatori ai record permettono di semplificare alcune operazioni (effettuate solo sull'indice, senza accedere al file se non quando indispensabile)
			\end{itemize}
		}
		
		\paragraph{Indici multilivello}
		Gli indici sono file essi stessi e quindi ha senso costruire indici sugli indici, per evitare di fare ricerche fra blocchi diversi. Possono esistere più livelli fino ad avere il livello più alto con un solo blocco. I livelli sono di solito abbastanza pochi, perché l'indice è ordinato, quindi l'indice sull'indice è sparso, e i record dell'indice sono piccoli.
			
		\begin{figure}[h!]
			\centering
			\includegraphics[width=0.7\linewidth]{graphics/screenshot010}
		\end{figure}
		
		Tutte le strutture di indice viste finora sono basate su strutture ordinate e quindi sono poco flessibili in presenza di elevata dinamicità. Gli indici utilizzati dai DBMS sono più sofisticati. Si usano strutture al albero dinamiche. Sono efficienti in presenza di aggiornamenti.
			
		\subsection{Strutture ad albero dinamiche}	
		Ogni albero è caratterizzato da un \textbf{nodo radice}, vari \textbf{nodi intermedi}, e vari \textbf{nodi foglia}. Ogni nodo coincide con una \textit{pagina o blocco} a livello di file system e di gestore del buffer. Ogni nodo ha un numero di figli abbastanza grande che dipende dall'ampiezza della pagina. I legami tra i nodi vengono stabiliti da \textit{puntatori} che collegano fra loro le pagine.
		
		\mybox{	Requisito importante per il buon funzionamento di queste strutture è che devono essere \textbf{bilanciate}, ovvero che la \textit{lunghezza del cammino che collega la radice alle foglie è \textbf{costante}}, ovvero che il tempo di accesso è uguale per tutte le informazioni.}	
		
		\subsubsection{Albero binario di ricerca}
		È un albero binario etichettato in cui per ogni nodo il \textit{sottoalbero sinistro} contiene solo \textbf{etichette} \textit{minori di quella del nodo} e il \textit{sotttoalbero destro} etichette maggiori. \textbf{Tempo di ricerca ed inserimento pari alla profondità.} Logaritmico nel caso "medio".
		
		\begin{figure}[h!]
			\centering
			\includegraphics[width=0.5\linewidth]{graphics/screenshot011}
		\end{figure}
		
		\subsubsection{Albero di ricerca di ordine Q}
		Ogni nodo ha fino a \textbf{Q figli} e fino a \textbf{Q-1} etichette, ordinate.
		
		Nell'i-esimo sottoalbero abbiamo tutte etichette magiori della (i-1)-esima etichetta e minori della i-esima.
			
		\begin{center}
			\includegraphics[width=0.7\linewidth]{graphics/screenshot012}
		\end{center}
		
		\subsubsection{B-Tree}
		Nei B-Tree i \textit{nodi intermedi possono avere puntatori direttamente ai dati.} Ogni nodo ha la struttura seguente: \large \begin{equation*}
			[p_0 (k_1,r_1) p_1 ... p_{j-1}(k_j,r_j) p_j]
		\end{equation*} \normalsize
		dove $r_i$ è un indirizzo al record di chiave $k_i$ e $p_i$ è un puntatore ad un figlio. \begin{itemize}
			\item Le chiavi $k_i$ sono in ogni nodo sono ordinate. 
			\item Ogni nodo non foglia con \textit{j} chiavi ha \textit{j+1} figli
			\item Tutte le chiavi associate ad un figlio tramite il puntatore $p_i$ sono comprese \textbf{strettamente} tra $k_i$ e $k_{i+1}$. 
			\item Ogni chiave fa riferimento ad un indirizzo, quindi nell'albero non ci sono chiavi duplicate.
		\end{itemize}
		
		\noindent
		\begin{minipage}{0.48\linewidth}
			\centering
			\includegraphics[width=\linewidth]{graphics/screenshot013}
			\captionof{figure}{B-Tree con riferimento a valori intermedi}
		\end{minipage}
		\hfill
		\begin{minipage}{0.48\linewidth}
			\centering
			\includegraphics[width=\linewidth]{graphics/screenshot014}
			\captionof{figure}{Estratto di B-tree di ordine 5}
		\end{minipage}
		
		\vspace{1em}
		\textbf{Definizione di B-Tree di ordine \textit{m} (con m $\geq 3$)} - \textit{ORDINE =  FAN-OUT}\begin{itemize}
			\item La radice ha \begin{align*}
				0 \leq &\textit{ nodi figli } \leq m \\
				1 \leq &\textit{ numero chiavi } \leq m-1
			\end{align*}
			\item Ogni nodo interno ha \begin{align*}
				\lceil m/2 \rceil &\leq \textit{ numero di figli } \leq m \\
				\lceil m/2 \rceil -1 &\leq \textit{ numero di chiavi } \leq m-1
			\end{align*}
			\item Tutte le foglie si trovano allo stesso livello
		\end{itemize}
			
		\paragraph{Inserimento in B-tree}
		Si verifica se la chiave è presente, altrimenti si scende alla foglia appropriata. Se il nodo non è completo (cioè contiene meno di m-1 chiavi), allora vi si può inserire il record. Se il nodo è completo, si crea un altro nodo (splitting) e si considera il valore mediano tra le chiavi del nodo completo più la chiave da inserire: \begin{enumerate}
			\item Il valore mediano si inserisce nel nodo padre
			\item Se il padre è completo si esegue la suddivisione (splitting) del padre (processo ricorsivo)
		\end{enumerate}
		
		\paragraph{Inserimento in nodo saturo}
		Se il nodo p' è saturo, cioè contiene m-1 chiavi, si esegue lo \textbf{splitting} del nodo:\begin{itemize}
			\item Si crea un altro nodo p'' in cui si memorizzano le chiavi maggiori della chiave di \textit{valore mediano} (oppure di posto mediano) $k_c$ nella sequenza (considerando chiavi del nodo e chiave da inserire) togliendole dal nodo originario
			\item La chiave di valore mediano $k_c$ si inserisce nel nodo padre, o meglio si inserisce \begin{align*}
				... p' (k_c,r_{kc}) p'' ...
			\end{align*}
		e se il padre è completo si esegue la suddivisione (splitting) del padre e così via fino alla radice.
		\end{itemize}
		
		\noindent
		\begin{minipage}{0.48\linewidth}
			\centering
			\includegraphics[width=\linewidth]{graphics/screenshot015}
		\end{minipage}
		\hfill
		\begin{minipage}{0.48\linewidth}
			\centering
			\includegraphics[width=\linewidth]{graphics/screenshot016}
		\end{minipage}
		
		\paragraph{Cancellazione da B-tree di chiave K}
		\begin{enumerate}[start=0]
			\item La chiave k da cancellare sia nel nodo n
			\item Se n è una foglia, si cancella k da n e si va al passo 3
			\item Se n NON è una foglia si sostituisce k prendendo il più piccolo (grande) fra gli elementi maggiori (minori) di k; tale chiave sostitutiva è in una fogli f, per costruzione dell'albero, e quindi le va applicato il punto 1.
			\item Controllo il numero di chiavi nella foglia: \begin{itemize}
				\item Se $\lceil m/2 \rceil -1 \leq \textit{ numero chiavi } \leq m-1$ allora fine cancellazione 
				\item altrimenti bisogna concatenare o bilanciare
			\end{itemize}
 		\end{enumerate}	
			
		\begin{figure}[h!]
			\centering
			\caption{Cancellazione della chiave 66}
			\includegraphics[width=0.7\linewidth]{graphics/screenshot017}
		\end{figure}
		
		Segue un esempio di cancellazione con concatenazione:
		
		\newpage
		\begin{figure}[h!]
			\centering
			\includegraphics[width=0.7\linewidth]{graphics/screenshot018}
		\end{figure}
		
		\begin{figure}[h!]
			\centering
			\includegraphics[width=0.7\linewidth]{graphics/screenshot019}
		\end{figure}
		
		\begin{figure}[h!]
			\centering
			\includegraphics[width=0.7\linewidth]{graphics/screenshot020}
		\end{figure}
		
		\newpage
		\paragraph{Esercizio} \textit{Dato il seguente B-Tree (Fan-Out = 4) eseguire le operazioni indicate mantenendo l'albero bilanciato:} B-Tree di ordine 4 (m = 4), quindi $1 \leq \textit{num Chiavi} \leq 3$.
		\begin{center}
			\includegraphics[width=0.7\linewidth]{graphics/screenshot021}
		\end{center}
		\begin{figure}[h!]
			\centering
			\caption{\textbf{Inserimento di k = 48}, inserimento a nodo NON saturo}
			\includegraphics[width=0.7\linewidth]{graphics/screenshot022}
		\end{figure}
		
		\begin{figure}[h!]
			\centering
			\caption{\textbf{Inserimento di k = 8} (inserimento a nodo saturo) 8 -12 -18 -21 Necessario SPLIT del nodo}
			\includegraphics[width=0.7\linewidth]{graphics/screenshot023}
		\end{figure}
		
		\begin{figure}[h!]
			\centering
			\caption{Suddivisione dell’informazione 8 – 12 – 18 – 21 in due 8 – 12 e 21 Aggiungiamo 18 al padre, facendo crescere di uno il numero dei puntatori $\to$ SPLIT del padre (18 – 28 – 50 – 71 in 18 – 28 e 71 più 50 nel padre)}
			\includegraphics[width=0.7\linewidth]{graphics/screenshot024}
		\end{figure}
		\newpage
		\begin{figure}[h!]
			\centering
			\caption{\textbf{Rimozione di k = 56}}
			\includegraphics[width=0.7\linewidth]{graphics/screenshot025}
		\end{figure}
		
		\begin{figure}[h!]
			\centering
			\caption{\textbf{Rimozione di k = 28}}
			\includegraphics[width=0.7\linewidth]{graphics/screenshot026}
		\end{figure}
		
		\begin{figure}[h!]
			\centering
			\caption{Bilanciamento della rimozione}
			\includegraphics[width=0.7\linewidth]{graphics/screenshot027}
		\end{figure}
		
		\begin{figure}[h!]
			\centering
			\caption{\textbf{Rimozione di k = 12}}
			\includegraphics[width=0.7\linewidth]{graphics/screenshot028}
		\end{figure}
		
		\newpage
		\begin{figure}[h!]
			\centering
			\caption{\textbf{Rimozione di k = 21}}
			\includegraphics[width=0.7\linewidth]{graphics/screenshot029}
		\end{figure}
		
		\begin{figure}[h!]
			\centering
						\caption{\textbf{Rimozione di k = 71}}
			\includegraphics[width=0.7\linewidth]{graphics/screenshot030}
		\end{figure}
		
		\begin{figure}[h!]
			\centering
			\caption{Passaggio intermedio}
			\includegraphics[width=0.7\linewidth]{graphics/screenshot031}
		\end{figure}
		
	\begin{figure}[h!]
		\centering
		\caption{Bilanciamento}
		\includegraphics[width=0.7\linewidth]{graphics/screenshot032}
	\end{figure}
		
		\newpage
	\subsubsection{B+ Tree}
	
	\noindent
	\begin{minipage}{0.45\linewidth}
		\begin{itemize}
			\item Le \textbf{foglie} sono \textbf{collegate in una lista}
			\item I puntatori ai dati sono \textbf{solo} nelle foglie
			\item L'inserimento delle chiavi avviene nei nodi foglia
			\item Ottimi per le ricerche su intervalli
			\item Molto usati nei DBMS
		\end{itemize}
	\end{minipage}
	\hfill
	\begin{minipage}{0.45\linewidth}
		\centering
		\includegraphics[width=\linewidth]{graphics/screenshot033}
	\end{minipage}
	
	\textbf{Organizzazione dei nodi:}
	\begin{figure}[h!]
		\centering
		\includegraphics[width=0.7\linewidth]{graphics/screenshot034}
	\end{figure}
	
	\paragraph{Inserimento di una chiave}
	Non provoca problemi quando la nuova chiave si può inserire in una \textit{foglia non satura}. Quando la foglia non ha spazio disponibile si esegue lo SPLIT della foglila: \begin{itemize}
		\item Si suddivide l'informazione presente nella foglia e la nuoa informazione in due (in modo equilibrato) allocando due foglie al posto di una.
		\item Il valore mediano delle chiavi si \textbf{si inserisce anche in copia nel padre}
	\end{itemize}
	Lo SPLIT causa il crescere del numero dei puntatori al livello superiore. Lo SPLIT può dover essere ripetuto sul nodo padre. Lo SPLIT del padre (nodo non foglia) viene gestito come per i BTree:\begin{itemize}
		\item Si suddivide l'informazione in due, mettendo le chiavi maggiori del valore mediano nel nuovo nodo
		\item Il valore mediano delle chiavi viene spostato nel padre
	\end{itemize}
	
	\paragraph{Rimozione di una chiave}
	Se la rimozione della chiave lascia due nodi foglia con spazio inutilizzato da consentire che tutta l'informazione in esse presente possa essere concentrata in una sola foglia si effettua un'operazione di \textbf{merge}, che richiede la modifica della disposizione dei puntatori. \textbf{MERGE} causa il decrescere del numero dei puntatori al livello superiore, e può dover essere ripetuto sul nodo padre.
		
	\mybox{\textbf{Bilanciamento:} se le modifiche causano differenze tra le lunghezze dei cammini tra radice e foglie è opportuo ri-bilanciare l'albero.}
	
	\newpage
	\textbf{Esercizio B+Tree - Fan-Out = 4}
	
	\begin{figure}[h!]
		\centering
		\includegraphics[width=0.7\linewidth]{graphics/screenshot036}
	\end{figure}
	
	\begin{figure}[h!]
		\centering
		\caption{\textbf{Inserimento k = 5}}
		\includegraphics[width=0.6\linewidth]{graphics/screenshot037}
	\end{figure}
	
	\begin{figure}[h!]
		\centering
		\caption{\textbf{Rimozione di k = 8}}
		\includegraphics[width=0.6\linewidth]{graphics/screenshot038}
	\end{figure}
	
	\begin{figure}[h!]
		\centering
		\caption{\textbf{Rimozione di k = 20}}
		\includegraphics[width=0.6\linewidth]{graphics/screenshot039}
	\end{figure}
	
	\begin{figure}[h!]
		\centering
		\caption{\textbf{Inserimento di k = 2}. Suddivisione dell’informazione 1 – 2 – 3 – 5 in due 1 – 2 e 3 – 5. Inseriamo 3 anche nel padre, facendo crescere di uno il numero dei puntatori}
		\includegraphics[width=0.7\linewidth]{graphics/screenshot040}
	\end{figure}
	
	\newpage
	\begin{figure}[h!]
		\centering
		\caption{\textbf{Inserimento di k = 10}}
		\includegraphics[width=0.6\linewidth]{graphics/screenshot041}
	\end{figure}
			
\textit{	Con l'inserimento di k = 6 abbiamo un inserimento in foglia satura con SPLIT ripetuto sul padre, è quindi necessario fare il primo SPLIT della foglia con 3 - 5 - 6 - 10.}
	\begin{figure}[h!]
		\centering
				\includegraphics[width=0.7\linewidth]{graphics/screenshot042}
		\caption{Suddivisione dell’informazione 3 – 5 – 6 – 10 in due 3 – 5 e 6 – 10 Inseriamo 6 anche nel padre $\to$ Chiavi nel padre 3 – 6 – 12 – 15 $\to$ SPLIT del padre Il padre non è foglia, quindi lo SPLIT viene gestito come nei B Tree $\to$ 3 – 6 – 12 – 15 in 3 – 6 e 15 e spostiamo 12 nel padre}

	\end{figure}
	
	\begin{figure}[h!]
		\centering
		\caption{\textbf{Rimozione di k = 2}}
		\label{fig:screenshot043}
		\includegraphics[width=0.6\linewidth]{graphics/screenshot043}
	\end{figure}
	
			
	\begin{figure}[h!]
		\centering
		\caption{Fusione}
		\label{fig:screenshot044}
		\includegraphics[width=0.6\linewidth]{graphics/screenshot044}
	\end{figure}
	
	\newpage
	\paragraph{Esercizio costruzione B+Tree}
	Data la seguente lisa di valori chiave $L = (1,6,2,9,4,7,11,8,13)$:\begin{enumerate}
		\item Costruire un possibile B+Tree (fan-out = 4) che contenga i seguenti nodi foglia: $(1,2,4),(6,7),(8,9),(11,13)$
		\item Mostrare l'albero dopo l'inserimento del valore chiave 3
	\end{enumerate}
	
	\begin{figure}[h!]
		\centering

		\includegraphics[width=0.7\linewidth]{graphics/screenshot045}
				\caption{N.B. non rappresentiamo i collegamenti tra le foglie}
	\end{figure}
	
	Per l'inserimento di k = 3 è necessario lo SPLIT della foglia e del suo nodo padre.
	\begin{figure}[h!]
		\centering
		\caption{\textbf{Insermento di k = 3} Suddivisione dell’informazione della foglia 1 – 2 – 3 – 4 in due 1 – 2 e 3 – 4 Aggiungiamo 3 al padre, facendo crescere di uno il numero dei puntatori $\to$ SPLIT del padre (nodo non foglia) $\to$ 3 – 6 – 8 – 11 in due 3 – 6 e 11 e spostiamo 8 nel padre}
		\includegraphics[width=0.7\linewidth]{graphics/screenshot046}
	\end{figure}
	
	\paragraph{Esercizio 2 da risolvere:} 
	Data la seguente lista di valori chiave $L = (1,2,4,7,10,11,15,16,19,20)$
	\begin{enumerate}
		\item Costruire un possibile B+Tree con fan-out = 4 che contenga i seguenti nodi foglia: $(1,2,4),(7,10),(11,15),(16,19,20)$
		\item Mostrare l'albero dopo l rimozione del valore chiave \textit{2}
		\item Mostrare l'albero dopo l'inserimento del valore chiave 18.
	\end{enumerate}
	\newpage
	\section{Gestione delle transazioni}	
	Riguardano aspetti tecnologici che caratterizzano il funzionamento dei DBMS riguardo la modalità con cui viene garantita la possibilità di conservare a lungo
	termine e in modo corretto il contenuto delle BD anche in presenza malfunzionamenti e guasti e di accessi concorrenti.
	
	\mybox{\textbf{Transazione:} Identifica una unità di lavoro elementare svolta da	un'applicazione.
		
	Caratteristiche di: \begin{itemize}
		\item \textbf{Correttezza}: La correttezza garantisce che il database passi da uno stato consistente a un altro stato consistente, rispettando tutti i vincoli di integrità (chiavi, vincoli di dominio, ecc.).
		\item \textbf{Robustezza}: La robustezza riguarda la capacità del sistema di mantenere le modifiche apportate dalle transazioni confermate (commit), anche in presenza di guasti hardware o software (es. blackout).
		\item \textbf{Isolamento}:L'isolamento assicura che transazioni concorrenti (eseguite contemporaneamente da più utenti) non interferiscano tra loro, comportandosi come se venissero eseguite in modo sequenziale.
	\end{itemize}
	
	Caratterizzata da:\begin{itemize}
		\item \textbf{Inizio} \emph{begin-transaction / start transaction}
		\item \textbf{Fine} \emph{end-transaction}
		
	\end{itemize}
	
	al cui interno deve essere eseguito una e una sola volta uno dei seguenti comandi: \begin{itemize}
		\item \emph{commit work}: per terminare correttamente.
		\item \emph{rollback work}: per abortire la transazione.
	\end{itemize} 
		}
			
		Un sistema transazionale (On-Line Transaction Processing) è in grado di definire ed eseguire transazioni per conto di un certo numero di applicazioni concorrenti.
		\begin{figure}[h!]
			\centering
			\includegraphics[width=0.5\linewidth]{graphics/screenshot047}
			\caption{Esempio di transazione con varie decisioni}
		\end{figure}
		\subsection{Proprietà ACID delle transazioni}
		Il codice che viene eseguito all’interno di una transazione gode di proprietà dette \textbf{ACIDE}.
		\subsubsection{Atomicità}
		Una transazione è un'unità atomica (indivisibile) di elaborazione: o vengono resi visibili tutti gli effetti di una transazione oppure la transazione non deve avere alcun effetto. $\to$ \textbf{Approccio TUTTO o NIENTE}. Non può lasciare la base di dati in uno stato intermedio:\begin{itemize}
			\item un guasto o un errore \textit{prima del commit} debbono causare	l'annullamento (\textbf{UNDO}) delle operazioni svolte.
			\item un guasto o errore dopo il commit non deve avere conseguenze, se necessario vanno ripetute (\textbf{REDO}) le operazioni.
		\end{itemize}
		Esito:\begin{itemize}
			\item \textbf{Commit} =  caso normale e più frequente.
			\item \textbf{Abort o rollback}: può essere richiesto dall'applicazione (suicidio) o può essere richiesto dal sistema per violazione dei vincoli, concorrenza, incertezza in caso di fallimento (omicidio).
		\end{itemize}
		\subsubsection{Consistenza}
		La consistenza richiede che l’esecuzione della transazione non violi i vincoli di integrità definiti sulla BD.  Quando il sistema rileva che una transazione sta violando uno dei vincoli (es. inserimento di tupla con chiave già presente) il sistema interviene per annullare la transazione o correggere la
		violazione del vincolo.
		La verifica dei vincoli di integrità può essere
		\begin{itemize}
			\item Immediata (nel corso della transazione)
			\item Differita (alla conclusione della transazione)
		\end{itemize}
		La consistenza garantisce che se lo stato iniziale è corretto anche lo stato finale è corretto,
		\subsubsection{Isolamento}
		L’isolamento richiede che l’esecuzione di una transazione sia indipendente dall’esecuzione contemporanea di altre transazioni. La transazione non risente degli effetti delle altre transazioni concorrenti: l'esecuzione concorrente di una collezione di transazioni deve \textbf{produrre un risultato che si potrebbe ottenere con una esecuzione sequenziale}.
		
		\textbf{Conseguenza:} Una transazione non espone i suoi stati intermedi. Si evita l' "effetto domino".
		
		\subsubsection{Durabilità (persistenza)}
		Gli effetti di una transazione andata in commit non	vanno perduti ("durano per sempre"), anche in	presenza di guasti. (Commit significa impegno). Un DBMS deve garantire che nessun dato venga perso per nessun motivo.
		
		\subsection{Gestore delle transazioni}
		Per ciascuna delle proprietà sopra elencate, vi sono dei moduli che verificano che siano applicate: \begin{itemize}
			\item \textbf{Atomicità e durabilità}: Gestore dell'affidabilità (Reliability manager)
			\item \textbf{Isolamento}: Gestore della concorrenza
			\item \textbf{Consistenza}: La CONSISTENZA è gestita dai compilatori del DDL che introducono controlli sui dati e procedure di verifica eseguite dalle transazioni.
		\end{itemize}
		
		\begin{figure}[h!]
			\centering
			\includegraphics[width=0.6\linewidth]{graphics/screenshot048}
		\end{figure}
		
		\textit{Domanda d'esame: come gestisce il DBMS le transazioni?}
		
		\subsubsection{Gestore dell'affidabilità}
		Assicura\textbf{ atomicità e durabilità} (persistenza). Gestisce l'esecuzione dei comandi transazionali: \emph{start transaction (B, begin), commit work (C), rollback work (A, abort)} 
		 e le operazioni di ripristino (recovery) dopo i guasti: \emph{ripresa a caldo e ripresa a freddo (warm restart e cold restart)}.		 
		 Usa il log: un archivio permanente che registra le operazioni svolte dal DBMS. Ogni azione di scrittura sulla BD viene protetta tramite una azione sul	LOG.
		 
		 \begin{figure}[h!]
		 	\centering
		 	\includegraphics[width=0.7\linewidth]{graphics/screenshot049}
		 	\caption{Architettura del gestore dell'affidabilità}
		 \end{figure}
		 
		 Il gestore dell’affidabilità deve disporre di \textbf{MEMORIA STABILE} cioè di memoria che risulti resistente ai guasti:
		\begin{itemize}
			\item Memoria centrale: non è persistente
			\item Memoria di massa: è persistente ma può danneggiarsi
			\item Memoria stabile: memoria che non può danneggiarsi
		\end{itemize}
		 è una \textbf{astrazione}, nessuna memoria può avere probabilità nulla di fallimento, viene perseguita attraverso la \textit{ridondanza:} dischi replicati, nastri, ...
		  Un guasto della memoria stabile è considerato catastrofico
		  
		 \subsubsection{Il log}
		 \textit{Domanda d'esame: cos'è il log e cosa vi è contenuto dentro?}
		 
		 Il log è un file sequenziale gestito dal gestore dell'affidabilità, scritto in memoria stabile. Permette di ricostruire il contenuto della BD a seguito di guasti. "Diario di bordo": riporta tutte le operazioni in ordine.
		 
		  Record nel log: \begin{itemize}
		  	\item \textbf{Operazioni delle transazioni:} Registrano le attività svolte da ciascuna transazione nell’ordine in cui vengono	effettuate.\sitemize{
		  	\item \emph{begin, B(T)}
		  	\item \emph{insert, I(T,O,AS)}
		  	\item \emph{delete, D(T,O,BS)}
		  	\item \emph{update, U(T,O,BS,AS)}
		  	\item \emph{commit, C(T), abort, A(T)}
		  	}
		  	
		  	Dove
		  	
		  	\mybox{\sitemize{\item T = id della transazione
		  			\item O = id oggetto coinvolto
		  			\item AS = valore dell’oggetto dopo (AFTER) la modifica
		  			\item BS = valore dell’oggetti prima
		  			(BEFORE) della modifica}}
		  	
		  	\item \textbf{Record di sistema:} Indicano l’effettuazione di operazioni specifiche del gestore dell’affidabilità:
		  	\sitemize{\item \emph{dump}: Copia completa e consistente della BD (effettuata quando il sistema non è operativo) = BACKUP
		  	\item \emph{checkpoint}: Operazione che viene svolta periodicamente ed ha l’obiettivo di registrare quali transazione sono attive}
		  \end{itemize}
		
		Il log serve "a ricostruire" le operazioni: checkpoint e dump servono ad evitare che la ricostruzione debba partire dall'inizio dei tempi si usano con riferimento a tipi di guasti diversi.
		
		\begin{figure}[h!]
			\centering
			\includegraphics[width=0.7\linewidth]{graphics/screenshot050}
		\end{figure}
		
		\subsubsection{UNDO e REDO}
		Operazioni specifiche per disfare e rifare le operazioni
		sulla BD, sono di competenza del gestore dell’affidabilità.
		\begin{itemize}
			\item \textbf{UNDO} di una azione su un \textbf{oggetto O}:\sitemize{\item \emph{Update, Delete}: ricopiare il valore del \textbf{before state (BS)} nell'oggetto O
			\item \emph{Insert:} eliminare O}
			\item \textbf{REDO} di una azione su un \textbf{oggetto O}:\sitemize{\item \emph{Update, Delete:} Ricopiare il valore dell'after state (AS) nell'oggetto O \item \emph{Delete:} Eliminare O}			
		\end{itemize}
		\textbf{Idempotenza di Undo e Redo:}\begin{itemize}
				\item \emph{undo(undo(A)) = undo(A)} 
				
				\item \emph{redo(redo(A)) = redo(A)}
			\end{itemize}
		
		L’effettuazione di un numero arbitrario di UNDO e REDO della stessa azione equivale allo svolgimenti di tale azione una sola volta. Questa proprietà è utile nel caso in cui si abbiano
		errori durante le operazioni di ripristino e sia necessario ripetere UNDO e REDO.
		
		\subsubsection{Checkpoint}
		Operazione che serve a "fare il punto" della situazione, semplificando le successive operazioni di ripristino: ha lo scopo di registrare quali transazioni sono attive in un certo istante e dualmente di confermare che le altre o non sono iniziate o sono finite. Vi sono varie modalità, vediamo la più semplice: \begin{itemize}
			\item Si sospende l'accettazione di richieste di ogni tipo (scrittura,	inserimenti, ..., commit, abort)
			\item Si trasferiscono in memoria di massa (tramite force) tutte le pagine sporche relative a transazioni andate in commit
			\item Si registrano sul log in modo sincrono (force) gli identificatori delle transazioni in corso
			\item Si riprende l'accettazione delle operazioni
		\end{itemize}
		Così siamo sicuri che \begin{itemize}
			\item  Per tutte le transazioni che hanno effettuato il commit i dati sono in memoria di massa, quindi registrate nella BD in modo permanente
			\item Le transazioni "a metà strada" (non hanno ancora fatto
			COMMIT o ABORT) sono elencate nel checkpoint
		\end{itemize}
		
		\subsubsection{Dump}
		Copia completa ("di riserva", backup) della base di dati
		\begin{itemize}
			\item Solitamente prodotta mentre il sistema non è operativo
			\item Salvato in memoria stabile (es. Su nastro)
			\item Un record di dump nel log indica il momento in cui il log è stato effettuato (e dettagli pratici, file, dispositivo, ...)
		\end{itemize}
		
		\subsubsection{Esito di una transazione}
		L'esito di una transazione è determinato \underline{irrevocabilmente} quando viene scritto il \textbf{record di commit nel log in modo sincrono}, con una \textbf{force}. Un guasto prima di tale istante porta ad un UNDO di tutte le
		azioni, per ricostruire lo stato originario della base di dati. Un guasto successivo non deve avere conseguenze: lo stato finale della base di dati deve essere ricostruito, con redo se
		necessario. Record di abort possono essere scritti in modo	asincrono.
		
		\subsubsection{Regole fondamentali per il log}
		Durante il funzionamento normale delle transazioni
		il gestore dell’affidabilità deve garantire che siano
		seguite due regole; le regole definiscono i requisiti minimi che consentono di ripristinare la correttezza della BD a fronte di guasti:
		\begin{itemize}
			\item \textbf{Write-Ahead-Log (WAL):} \textit{Si scrive il giornale (parte before) prima del database. }
			
			Impone che la parte BEFORE STATE dei record di un Log venga scritta nel Log PRIMA di effettuare la corrispondente operazione sulla BD. Consente di disfare le scritture già effettuate in memoria di massa da parte di	una transazione che non ha ancora effettuato un COMMIT. Per ogni aggiornamento viene reso disponibile in modo affidabile il valore precedente la scrittura.
			
			\textit{Versione semplificata:} Scrivere prima record di Log e poi nel DB, scrivi il Log per primo.
			
			Scrive prima il record di Log e poi il record nella BD. Al COMMIT tutte le pagine della BD modificate dalla transazione sono scritte in memoria di massa. \textbf{Non richiede REDO}.
			
			Tecnica di aggiornamento immediato, permette comunque il recovery: se una transazione fallisce prima del COMMIT \textbf{l’effetto di eventuali	operazioni effettuate deve essere annullato}.
			
			\begin{figure}[h!]
				\centering
				\includegraphics[width=0.7\linewidth]{graphics/screenshot051}
			\end{figure}
		
			
			
			\item \textbf{Commit-Precedenza:} Si scrive il giornale (parte after) prima del commit.
			
			Impone che la parte AFTER STATE dei record di un Log venga scritta	nel Log PRIMA di effettuare il COMMIT. Consente di rifare le scritture già decise da una transazione che ha effettuato il COMMIT ma le cui pagine modificate non sono ancora state trascritte dal	\textit{Buffer Manager} in memoria di massa.
			
			\textit{Versione semplificata:} Record di Log scritti prima del COMMIT.
			
			 La scrittura dei record di Log precede le azioni sulla BD. Le azioni sulla BD avvengono dopo il COMMIT e la scrittura del record di COMMIT nel Log. \textbf{Non richiede UNDO}
			 Tecnica di aggiornamento differito: i dati non sono fisicamente aggiornati fino all’esecuzione del COMMIT. \textbf{Se la transazione fallisce non è necessario l’UNDO}.
			
			\begin{figure}[h!]
				\centering
				\includegraphics[width=0.7\linewidth]{graphics/screenshot052}
			\end{figure}
			
			\item Più generale e comunemente utilizzato è un \textbf{compromesso} tra le due regole sopra scritte: Le scritture sulla BD, una volta protette dalle opportune scritture sul Log, possono avvenire in un qualunque momento rispetto alla scrittura del record di COMMIT nel Log. Consente al Buffer manager di ottimizzare le operazioni.
			
			\textbf{Richiede sia UNDO che REDO.}
			
			\begin{figure}[h!]
				\centering
				\includegraphics[width=0.7\linewidth]{graphics/screenshot053}
			\end{figure}
			
			
		\end{itemize}
		
	\section{Gestione dei guasti}
	I guasti possono essere classificati in: \begin{itemize}
		\item \textbf{Guasti di Sistema (Guasti "soft")}: Errori di programma, crash di sistema, caduta di tensione. Si perde la memoria centrale (buffer), ma \textit{non si perde la memoria secondaria} (BD e Log). Richiede la \textbf{ripresa a caldo (warm restart)}.
		\item \textbf{Guasti di Dispositivo (Guasti "hard")}: Guasti sui dispositivi di memoria secondaria, si perde anche la memoria secondaria (BD), ma \textit{non si perde la memoria stabile }(e quindi il log). Richiede la \textbf{ripresa a freddo (cold restart)}.
	\end{itemize}
	
	Quando il Sistema individua un guasto (sia di Sistema che di dispositivo) immediatamente \textbf{forza un arresto }completo delle transazioni, e il successivo ripristino del corretto funzionamento del Sistema Operativo (\textbf{Boot}). Quindi viene attivata una procedura di ripresa \textit{A CALDO }se il guasto è di sistema, \textit{A FREDDO }se il guasto è di dispositivo. Al termine della procedura di ripresa, il sistema è
	di nuovo utilizzabile. Questo viene chiamato \textbf{Modello "fail-stop"}.
	\begin{figure}[h!]
		\centering
		\includegraphics[width=0.7\linewidth]{graphics/screenshot054}
	\end{figure}
	
\textbf{Obiettivi della procedura di ripresa}: Classificare le transazioni in \begin{itemize}
	\item \textbf{Completate} (tutti i dati in memoria stabile)
	\item \textbf{In commit} ma non necessariamente completate: Può servire REDO per rifare le azioni e garantire la consistenza
	\item\textbf{ Senza commit:} Vanno annullate con UNDO per lasciare la BD nel suo stato iniziale precedente alla esecuzione della transazione
\end{itemize}
Quest'ultime due sono transazioni potenzialmente attive: si classificano in base alla informazione presente nel Log.

\subsection{Ripresa a caldo}
 Garantisce l’atomicità e la persistenza delle transazioni.\begin{itemize}
 	\item  \textbf{Atomicità}: viene garantito che le transazioni in corso al momento del guasto lascino la BD nello stato iniziale o nello stato finale. \item \textbf{Persistenza}: per le transazioni completate ma non ancora trascritte in memoria di massa si prevede la scritture in memoria di massa
 \end{itemize}
 
 È divisa in quattro fasi:\begin{enumerate}
 	\item Trovare l'ultimo \textbf{CHECKPOINT} (ripercorrendo il Log
 	a ritroso)
 	\item \textbf{Costruire gli insiemi UNDO (transazioni da disfare) e REDO (transazioni da rifare)}
 	
 	Si inizializza l’insieme \textbf{UNDO} (transazioni da disfare) con le \textbf{transazioni attive al CHECKPOINT}. Si inizializza l’insieme REDO (transazioni da rifare)	con l’insieme vuoto. Si percorrere il log in avanti
 	\sitemize{\item aggiungendo ad \textbf{UNDO}\textbf{ tutte le transazioni per cui è
 		presente un record di BEGIN}
 		\item e \textbf{spostando da UNDO a REDO le transazioni per cui è
 		presente il record di COMMIT}}
 	
 	\item Ripercorrere il log all'indietro, fino alla più vecchia azione delle transazioni in UNDO e REDO,	disfacendo tutte le azioni delle transazioni in UNDO – Questa azione potrebbe precedere il record di
 	CHECKPOINT
 	\item Ripercorrere il log in avanti, rifacendo tutte le azioni
 	delle transazioni in REDO, nell’ordine in cui sono 	presenti nel Log
 \end{enumerate}
 
 \textbf{Esempio di ripresa a caldo:}
 \begin{figure}[h!]
 	\centering
 	\includegraphics[width=0.7\linewidth]{graphics/screenshot055}
 \end{figure}
 
 \newpage
 \noindent
 \begin{minipage}{0.48\linewidth}
 	\centering
 	\includegraphics[width=\linewidth]{graphics/screenshot056}
 \end{minipage}
 \hfill
  \begin{minipage}{0.48\linewidth}
 	\centering
 	\includegraphics[width=\linewidth]{graphics/screenshot057}
 \end{minipage}

  \begin{minipage}{0.48\linewidth}
 	\centering
 	\includegraphics[width=\linewidth]{graphics/screenshot058}
 \end{minipage}
 \hfill
 \begin{minipage}{0.48\linewidth}
 	\centering
 	\includegraphics[width=\linewidth]{graphics/screenshot059}
 \end{minipage}

\subsection{Ripresa a freddo}
La ripresa a freddo risponde ad un guasto che provoca il deterioramento di una parte della BD. È articolato in 3 fasi: \begin{itemize}
	\item  Si ripristinano i dati a partire dal backup (DUMP): Si accede al più recente record di DUMP nel Log
	\item Si \textbf{eseguono le operazioni registrate} sul Log fino all'istante del
	guasto: si ripercorre in avanti il Log, si eseguono sia le azioni sulla BD che le azioni di COMMIT e ABORT, riportandosi nella situazione precedente
	\item Si esegue una \textbf{ripresa a caldo}
\end{itemize}

\begin{figure}[h!]
	\centering
	\includegraphics[width=0.7\linewidth]{graphics/screenshot060}
\end{figure}

\textit{Domanda d'esame: si enunci la differenza fra la ripresa a caldo e la ripresa a freddo.}
 

\section{Gestore della concorrenza}
Un DBMS spesso deve servire diverse applicazioni e rispondere alle richieste di diversi utenti. Il numero di transazioni è molto elevato, quindi è indispensabile che le transazioni di un DBMS vengano eseguite CONCORRENTEMENTE, la loro esecuzione SERIALE è impensabile.			
			
In gestore della concorrenza riceve le richieste di accesso ai dati e decide se autorizzarle o meno, eventualmente riordinandole. Il gestore della concorrenza stabilisce quindi
l’ordine degli accessi chiamato \textbf{SCHEDULER}			
			
			
\begin{figure}[h!]
	\centering
	\includegraphics[width=0.7\linewidth]{graphics/screenshot061}
\end{figure}

Decine o centinaia di transazioni al secondo (Transactio Per Second - TPS \textbf{non possono essere seriali}. I TPS sono l'unità di misura utilizzata per caratterizzare il carico applicativo di un DBMS. 

\textit{Domanda d'esame: si descrivano brevemente le anomalie delle transazioni concorrenti}

\subsection{Anomalie delle transazioni concorrenti}			
L’esecuzione concorrente delle transazioni può causare problemi di correttezza. 
\subsubsection{Perdita di aggiornamento}
Due transazioni identiche che operano sullo stesso oggetto x della BD:
\begin{itemize}
	\item $t_1: r_1(x),x = x+1, w_1(x)$ dove $r_1(x) \to$ lettura dell'oggetto x da parte della transazione $t_1$ e $w_1(x)\to$ lettura dell'oggetto x da parte della transazione $t_1$
	\item $t_2:r_2(x),x = x+1,w_2(x)$
\end{itemize}
Se ad esempio, inizialmente $x=2$, dopo una esecuzione seriale x = 4. 
\begin{figure}[h!]
	\centering
	\includegraphics[width=0.7\linewidth]{graphics/screenshot063}
\end{figure}
Viceversa, per una esecuzione concorrente un aggiornamento viene perso, poiché alla fine di questa esecuzione $x=3$: entrambe le transazioni leggono 2 come valore iniziale di x, gli effetti della transazione $t_2$ (la prima che scrive il nuovo valore di x) vengono persi.
\begin{figure}[h!]
	\centering
	\includegraphics[width=0.5\linewidth]{graphics/screenshot064}
\end{figure}

\newpage
\subsubsection{Lettura sporca}
\textbf{Aspetto critico:} una transazione legge uno stato intermedio (sporco) e lo può comunicare all'esterno.

\begin{figure}[h!]
	\centering
	\includegraphics[width=0.7\linewidth]{graphics/screenshot065}
\end{figure}

L'unico modo per ripristinare la correttezza a seguito dell'ABORT di $t_1$ sarebbe di \textbf{imporre l'ABORT} di $t_2$ e ricorsivamente di tutte le transazioni che avessero letto dati modificati da $t_2$, con un \textit{effetto domino}; è una soluzione costosa da gestire e talvolta impraticabile, ad esempio se $t_2$ avesse comunicato il dato all'esterno. Quindi \textbf{le transazioni non devono esporre dati intermedi.}

\subsubsection{Letture inconsistenti}
La transazione legge valori diversi per lo stesso oggetto x senza modificarlo. Sarebbe opportuno che una transazione che accede due volte alla BD trovi esattamente lo stesso valore per ciascun dato letto.\textbf{ La transazione non deve risentire dell’effetto di altre transazioni}.

\begin{figure}[h!]
	\centering
	\includegraphics[width=0.7\linewidth]{graphics/screenshot066}
\end{figure}
\subsubsection{Aggiornamento fantasma}
\begin{figure}[h!]
	\centering
	\includegraphics[width=0.7\linewidth]{graphics/screenshot067}
\end{figure}

\subsection{Controllo di Concorrenza}
\begin{figure}[h!]
	\centering
	\includegraphics[width=0.7\linewidth]{graphics/screenshot068}
\end{figure}

La garanzia dell’isolamento (quindi anche di non esporre stati intermedi) è molto onerosa. I DBMS prevedono la possibilità di specificare un grado di protezione ridotto: Alcune anomalie vengono evitate, altre anomalie possono manifestarsi. Si può rinunciare ad un controllo di concorrenza stringente per migliorare le prestazioni.

\paragraph{Modello formale di transazione} una transazione è definita come una \textbf{sequenza di azioni} di \textbf{lettura e scrittura:} $t_1 = r_1(x),r_1(y),w_1(x),w_1(y)$. Ogni transazione è un oggetto sintattico di cui si conoscono soltanto le operazioni di ingresso e uscita. Le operazioni nello schedule compaiono seguendo
l’ordine temporale con cui sono eseguite sulla BD. Il controllo di concorrenza ha la funzione di accettare alcuni schedule e di rifiutarne altri.

\subsubsection{Soluzioni iniziali NON applicabili}
Ipotesi semplificativa: inizialmente assumeremo di sapere l’esito (COMMIT/ABORT) delle
transazioni che appaiono in uno schedule. Potremo ignorare le transazioni che producono un ABORT, togliendo dallo schedule tutte le loro operazioni e ci concentreremo solo sulle transazioni che producono un COMMIT. In questo caso lo schedule si dice una
\textbf{commit-proiezione}. Questa assunzione permette uno studio teorico, ma non è accettabile in pratica. Lo schedule va accettato o rifiutato senza conoscere l’esito finale delle transazioni coinvolte.

\textbf{Idea base:} individuare classi di schedule serializzabili che siano sottoclassi
degli schedule possibili, siano serializzabili e la cui proprietà di serializzabilità sia verificabile a costo basso.

\paragraph{View-Equivalenza}Nozione di equivalenza concettualmente interessante,
ma inutilizzabile in pratica per ragioni computazionali. \begin{itemize}
	\item \textbf{LEGGE DA:} $r_i(x)$ \textbf{legge-da} $w_j(x)$ in uno schedule S, se $w_j(x)$ precede $r_i(x)$ in S e non c'è $w_k(x)$ fra $r_i(x)$ e $w_j(x)$ in S.
	\item \textbf{SCRITTUA FINALE:} $w_i(x)$ in uno schedule S è \textbf{scrittura finale} se è l'ultima scrittura dell'oggetto x in S.
\end{itemize}

Due schedule sono \textbf{view-equialenti} $S_i \approx_V S_j$  se hanno la \textbf{stessa relazione legge-da e le stesse scritture finali.}
\paragraph{View Serializzabilità} Uno schedule è view-serializzabile se esiste uno
schedule seriale view-equivalente ad esso. L'insieme degli schedule view-serializzabili è indicato con \textbf{VSR}.
\begin{figure}[h!]
	\centering
	\includegraphics[width=0.7\linewidth]{graphics/screenshot069}
\end{figure}

\begin{figure}[h!]
	\centering
	\includegraphics[width=0.7\linewidth]{graphics/screenshot070}
\end{figure}

Determinare la view-equivalenza di due schedule è un problema con complessità lineare. Determinare se uno schedule è view-equivalente ad un qualsiasi schedule seriale è un problema
NP-difficile. Il concetto di view-equivalenza non è utilizzabile in pratica per caratterizzare la serializzabilità/.Si preferisce definire una condizione di equivalenza più ristretta, che non copra tutti i casi di equivalenza tra schedule, ma sia utilizzabile nella pratica perché ha una complessità inferiore.

\paragraph{Conflict Equivalenza}
Nozione di equivalenza più facilmente utilizzabile, si basa sul concetto di \textbf{conflitto}. Definizione preliminare:\begin{itemize}
	\item Un'azione $a_i$ è in \textbf{conflitto} con $a_j$ $(i\neq j)$, se operano sullo stesso oggetto e almeno una di esse è in scrittura. Due casi: conflitto \textbf{read-write} (rw o  wr) o conflitto \textbf{write-write} (ww) 
\end{itemize}

Due schedule sono \textbf{conflict equivalenti} $S_i \approx_c S_j$ se presentano le \textbf{stesse operazioni} e \textbf{ogni coppia di operazioni in conflitto compare nello stesso ordine in entrambi.}

\paragraph{Conflict serializzabilità}
Uno schedule è \textbf{conflict-serializzabile} se è conflict-equivalente ad un qualche schedule seriale. L'insieme degli schedule conflict-serializzabili è indicato con \textbf{CSR}.

\newpage
\begin{figure}[h!]
	\centering
	\includegraphics[width=0.7\linewidth]{graphics/screenshot071}
\end{figure}

\begin{figure}[h!]
	\centering
	\includegraphics[width=0.7\linewidth]{graphics/screenshot072}
\end{figure}

La classe degli schedule CSR (conflict-serializzabili) è
strettamente inclusa in quella degli schedule VSR
(view-serializzabili)

\begin{figure}[h!]
	\centering
	\includegraphics[width=0.4\linewidth]{graphics/screenshot073}
\end{figure}

Anche la conflict-serializzabilità, pur più rapidamente verificabile è inutilizzabile in pratica: l'algoritmo, con opportune strutture dati richiede tempo lineare, però la conflict-serializzabilità risulta ancora eccessivamente onerosa in pratica. La tecnica sarebbe efficiente se potessimo conoscere il grafo dall’inizio, ma così non è: non è praticabile mantenere il grafo, aggiornarlo e verificarne l’aciclicità ad ogni richiesta di operazione.
			
\subsubsection{Soluzioni utilizzate in pratica: LOCKING}
Il meccanismo più noto e il primo usato dai DBMS si basa sul \textbf{LOCKING}. In questa soluzione tutte le operazioni di lettura e scrittura devono essere protette da opportune primitive:\begin{itemize}
	\item Tutte le letture sono precedute da \textbf{r\_lock} (lock condiviso) e seguite da \textbf{unlock}. Su un dato possono essere attivi più lock di questo tipo.
	\item Tutte le scritture sono precedute da \textbf{w\_lock} (lock esclusivo) e seguite da \textbf{unlock}. Non può coesistere con altri lock sullo stesso dato.
\end{itemize}

Quando una transazione segue queste regole si dice \textbf{ben formata rispetto ai lock}.
	
Quando una trasnazione prima legge e poi scrive un oggetto, può richiedere subito un lock esclusivo o chiedere prima un lock condiviso e poi uno esclusivo (lock escalation). Il gestore della concorrenza riceve le richieste di lock provenienti dalle transazioni e le gestisce sulla base della \textbf{tavola dei conflitti.}	

Se la risorsa non è concessa, la transazione richiedente è posta in attesa (eventualmente in coda), fino a quando la risorsa non diventa disponibile. Il \textbf{lock manager} gestisce una tabella dei lock, per ricordare la situazione dei lock già concessi.

\begin{figure}[h!]
	\centering
	\includegraphics[width=0.7\linewidth]{graphics/screenshot074}
\end{figure}

\subsubsection{Meccanismi di locking}
I meccanismi di lockig possono essere usati per garantire le transazioni che lavorano sulla BD accedano ai dati in \textbf{mutua esclusione}. Per avere la garanzia che le transazioni seguano uno schedule serializzabile è necessario porre una restrizione sull'ordinamento delle richieste di lock.

\paragraph{Principio di Locking a 2 Fasi - 2PL} Usato da quasi tutti i sistemi, garantisce "a priori" la conflict-serializzabilità. È basata su due regole: \begin{itemize}
	\item "proteggere" tutte le letture e scritture con \textbf{lock}
	\item un \textbf{vincolo} sulle richieste e i rilasci del lock: una transazione, dopo aver rilasciato un lock, non può acquisirne altri.
\end{itemize}

Si possono distinguere due fasi nell'esecuzione della transazione:\begin{enumerate}
	\item Una prima fase in cui si \textbf{acquisiscono i lock} per le risorse a cui si deve accedere - \textbf{FASE CRESCENTE} - Passaggio da r\_lock a w\_lock sta nella fase crescente.
	\item Una seconda fase in cui si rilasciano i lock acquisiti - \textbf{FASE CALANTE}.
\end{enumerate}
			
\begin{minipage}{0.48\linewidth}
	\centering
	\includegraphics[width=\linewidth]{graphics/screenshot075}
\end{minipage}		
\hfill
\begin{minipage}{0.48\linewidth}
Lo schedule 2Pl è un sistema in cui le \textbf{transazioni sono ben formate} rispetto ai locking, cioè richiedono il lock prima di accedere e lo rilasciano prima di terminare con un \textbf{Lock Manager} che rispetta la politica nella tabella e in cui le transazioni seguono il principio del locking a due fasi. È un sistema transazionale caratterizzato dalla serializzabilità delle proprie transazioni. La classe 2PL contiene gli schedule che soddisfano queste condizioni.
\end{minipage}

\newpage
\begin{figure}[h!]
	\centering
	\includegraphics[width=0.8\linewidth]{graphics/screenshot076}
\end{figure}

	
	\subsubsection{Locking a due fasi stretto}		
	Restrizione del protocollo 2PL, la condizione aggiuntiva è che \textbf{i lock possono essere rilasciati solo dopo il commit o abort}. Permette di rimuovere l'ipotesi \textit{commit-proiezione} ed elimina il rischio di letture sporche. I lock vengono rilasciati solo al termine della transazione, dopo che i dati sono stati portati nel loro stato finale. Versione di 2PL molto usaa nei DBMS commerciali.
	
			
	\subsubsection{Stallo (deadlock) - blocco critico}
	Avviene quando si hanno delle \textbf{attese incrociate}, ovvero quando due transazioni detengono ciascuna una risorsa e aspettano la risorsa detenuta dall'altra.
	Esempio: \begin{itemize}
		\item $t_1$: read(x), write(y)
		\item $t_2$: read(y), write(x)
		\item Schedule (2PL):
		$r\_lock_1(x), r\_lock_2(y), read_1(x), read_2(y) w\_lock_1(y), w\_lock_2(x)$
	\end{itemize}
Nessuna delle due transazioni riesce a procedure e il sistema è bloccato. $t_1$ è in attesa che si liberi y che è bloccato da $t_2$, mentre $t_2$ è in attesa che si liberi x che è bloccato da $t_1$. Ci sono tre soluzioni:\begin{enumerate}
	\item \textbf{Timeout:} tecnica molto semplcie usata in molti DBMS commerciali. Le transazioni rimangono in attesa per un tempo pregissato ragionevole, se passa questo tempo e la risorsa non è stata concess allora alla richiesta di lock viene data risposta negativa. Quindi la transazione in deadlock viene tolta dalla condizione di attesa e presumibilmente abortita. 
	\item \textbf{Rilevamento dello stallo:} questa tecnica prevede di \textit{non porre vincoli} al comportamento del sistema, ma di controllare il contenuto delle tabelle di lock tutte le volte che si ritiene necessario per rilevare eventuali situazioni di blocco. È oneroso e complicato; il rilevamento di un blocco critico richiede di analizzare le relazioni di attesa tra le varie transazioni e di determinare se esiste un ciclo.
	\item \textbf{Prevenzione dello stallo:} vi sono diverse tecniche per prevenire il blocco critico: \begin{enumerate}
		\item Richiedere il \textbf{lock di tutte le risorse} necessarie alla transazione in una sola volta. Problema: spesso le transazioni non conoscono a priori le risorse a cui vogliono accedere.
		\item Le transazioni acquisiscono un timestamp e viene consentita l’attesa di $t_i$ su una risorsa acquisita da $t_j$ solo se vale una certa relazione di precedenza tra i timestamp di $t_i$ e $t_j$ (esempio $i<j$)
		\item Si basa sull’osservazione che molti deadlock si verificano all’atto di
		"incrementare" un lock da lettura a scrittura: Due transazioni acquisiscono lock condivisi sullo stesso dato e poi cercano di incrementare il lock bloccandosi a vicenda. Soluzione: Introduzione di un nuovo tipo di lock detto di \textbf{UPDATE} richiesto per leggere un dato su cui successivamente si vuole scrivere.
	
	\end{enumerate}
\end{enumerate}

\textbf{UPDATE LOCK:} Il deadlock più frequente avviene quando due transazioni concorrenti vogliono prima leggere e poi scrivere la stessa risorsa. Per evitare questa situazione, i sistemi offrono gli update lock (UL). L’update lock viene acquisito da transazioni che vogliono inizialmente leggere un oggetto per poi modificarne il valore. Il lock di update è incompatibile con altri lock di update. In un certo istante una sola transazione può avere un lock di update su un dato. Il lock di update è compatibile con un lock di lettura. All’atto di passare alla scrittura la transazione deve comunque richiedere un lock in scrittura e quindi resta in attesa che eventuali altri lettori terminino la loro azione.
			
			
			
			
			
			
			
			\newpage
			\section{Possibili domande d'esame}
			\begin{itemize}
				\item \textbf{\textit{Si descrivano i moduli che compongono il DBSM descrivendo brevemente le funzioni di ciascuno:}} risposta nelllo schema sottostante.
				
				\begin{figure}[h!]
					\centering
					\includegraphics[width=0.7\linewidth]{graphics/screenshot002}
				\end{figure}
				
				Bisogna parlare di ciascun modulo brevemente, facendo l'esempio di una query dall'alto verso il basso e viceversa.
				
				\item \textbf{\textit{(da approfondire) Quali sono i moduli che mi permettono di gestire le transazioni in un DBMS?}} Significa gestire la concorrenza e gestire l'affidabilità.
				
					\begin{figure}[h!]
					\centering
					\includegraphics[width=0.7\linewidth]{graphics/screenshot003}
				\end{figure}
				
				\item \textit{\textbf{Si descriva in che modo le tuple vengono organizzate nelle pagine:}} 
			\end{itemize}
			
			
			%%% INTEGRA E RECUPERA LEZIONE PREC.
			B + Tree
			F Chiavi, ogni chiave è seguita da un puntatore. La chiave $K_1$ ha un puntatore prima di sè $P_0$, quindi abbiamo $F+1$ puntatori che si chiama \textbf{Fan Out dell'albero}. (Dato Fan Out =4, abbiamo 3 chiavi). I puntatori saranno sempre uno in più, il Fan Out dice il numero massimo di puntatori che posso avere. In questo caso il puntatore $P_0$ che sta prima della prima chiave punta al sottoalbero che contiene le chiavi $K<K_1$. $P_F$ punta al sottoalbero che contiene le chiavi $K \geq K_F$ mentre $P_i$ è il puntatore che punta al sottoalbero che contiene le chiavi $K_i \leq K < K_{i+1}$.
			Data la chiave di ricerca V, ad ogni nodo intermedio, se $V<K$ allora si segue il puntatore $P_0$, se $V\geq K_F$ si segue il puntatore $P_F$, altrimenti si segue il puntatore $P_j$ tale che $K_j \leq K < K_{j+1}$. Se l'indice è \textbf{primario} i nodi foglia contengono l'intera tupla, se l'indice è secondario i nodi foglia contengono i putnatori ai blocchi della BD che contengono le tuple. 
			
			\textbf{Inserimenti e cancellazioni:} nel caso di inserimento o eliminazione di una tupla devo risistemare l'albero affinché rispetti i vincoli. Se c'è posto nella foglia lo inserisco. Se arrivo in un nodo con numero di chiavi massimo devo dividere il nodo, splittarlo, in modo da inserirlo, ovviamente aggiungendo un nuovo puntatore. Eliminazione, c'è la possibilità di fare un merge. Se devo modificare il valore di una chiave, devo trattarle come eliminazioni seguite da inserimenti. 
\end{document} 